<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit's Page</title>
</head>
<body>
    <h1> Edit Product </h1>
    <form action="/products/<%= product._id %>?_method=PUT" method="post">
        <label for="name">Product's Name
            <input type="text" name="name" id="name" value="<%= product.name %>">
        </label>
        <label for="price">Product's Price
            <input type="text" name="price" id="price" value="<%= product.price %>">
        </label>
        <label for="category">Product's Category
            <select name="category" id="category">
                <option value="fruit" <%= product.category[0] === 'fruit' ? 'selected' : '' %> >fruit</option>
                <option value="vegetable" <%= product.category[0] === 'vegetable' ? 'selected' : '' %> >vegetable</option>
                <option value="diary" <%= product.category[0] === 'diary' ? 'selected' : '' %> >diary</option>
            </select>
        </label>
        <button type="submit">Submit</button>
    </form>
    <a href="/products/<%= product._id %>">Back to Show</a>
</body>
</html>