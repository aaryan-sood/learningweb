<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>random</title>
</head>
<body>
    <h1>The random number is : <%= num %></h1>
    <% if(num%2 === 0){ %>
        <h2>this is an even number</h2>
    <% } else {%>   <!-- Now be careful in the way if else is handled in ejs the else should be in the line as the colsing %> of the if block-->
    <%  %>
        <h2> this is an odd number</h2>
    <% } %>
    <h3> the number is : <%= num%2 === 0 ? 'Even' : 'odd' %> </h3>
</body>
</html> 